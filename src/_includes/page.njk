<!doctype html>
<html lang="ja">
<head>
  <meta charset="UTF-8"/>
  <meta rel="icon" type="image/png" href="{{ site.icon | htmlBaseUrl }}"/>
  <meta name="generator" content="{{ eleventy.generator }}" />
  <title>{{ title }} - UNTIL.</title>
</head>
<body>
  {% include "./components/header.njk" %}
  <main>
    {{ content | safe }}
  </main>
  {% include "./components/footer.njk" %}
</body>
</html>
